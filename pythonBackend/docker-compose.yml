services:
  db:
    image: postgres:15
    environment:
      POSTGRES_USER: WillH
      POSTGRES_PASSWORD: 12345
      POSTGRES_DB: hikingapp
    ports:
      - "5432:5432"
    volumes:
      - db-data:/var/lib/postgresql/data
volumes:
  db-data:
